-- Applied after migrations on `supabase db reset`.
-- Default categories live in the schema migration so they exist on hosted projects too.
-- Sample household/expense rows need real auth users — create those via the app
-- (Phase 2), then expand this file if you want local demo data.

SELECT 1;
